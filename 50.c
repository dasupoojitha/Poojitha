// c program to using simple if condition to check even numbers.
#include <stdio.h>
int main()
{
    int even = 0;
    printf("enter the value of even:");
    scanf("%d", &even);
    if (even % 2 == 0)
        printf("the even number");
return 0;
}                        